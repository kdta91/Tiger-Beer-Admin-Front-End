'use strict';

(function () {
    angular.module('app')
        .controller('WinController', ['$scope', '$state', 'UserSessionFactory', function ($scope, $state, UserSessionFactory) {

        }]);
})();